
FMOVE.EXE - Move a file from one dir
to another.
                                   
Use: FMOVE src dst

src and dst = [path/]filename

Notes:

If destination filename is not supplied
the source filename is used.

The directories in the paths must exist,
(they will not be created).
